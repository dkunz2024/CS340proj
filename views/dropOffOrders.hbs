<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Recycling Home Page</title>

    {{!-- add scipts --}}
    <script src="https://cdn.jsdelivr.net/npm/handlebars@latest/dist/handlebars.runtime.js"></script>
    <script src="http://ajax.googleapis.com/ajax/libs/jquery/1.9.1/jquery.min.js"></script>

    {{!-- add CSS styling --}}
    <link rel="stylesheet" href="style.css">

  </head>
  <body>

    {{!-- create a header that allows us to navigate to every other page --}}
    <header>
      <div class="header-image-container">
        <h1 class="site-title"><a href="?name=">Recyclopse</a></h1>
    
        <div class="nav-links">
          <a class="navbar-items" href = "/about">About Us</a>
          <a class="navbar-items" href = "/customers">Customers</a>
          <a class="navbar-items" href = "/dropOffOrders">Drop-Off Orders</a>
          <a class="navbar-items" href = "/salesOrders">Materials Orders</a>
          <a class="navbar-items" href = "/wasteOrders">Waste Shipments</a>
          <a class="navbar-items" href = "/wasteLocations">Waste Locations</a>
        </div>
      </div>
    </header>
    
    <main class="content">
        <div class ="table-div">
            {{!-- Create a table --}}
            <table>

                {{!-- Header section --}}
                <thead>

                    {{!-- For just the first row, we print each key of the row object as a header cell so we
                    know what each column means when the page renders --}}
                    <tr>
                        {{#each data.[0]}}
                        <th>
                            {{@key}}
                        </th>
                        {{/each}}
                        <th>delete</th>
                    </tr>
                </thead>

                {{!-- Body section --}}
                <tbody>

                    {{!-- For each row, print the id, fname, lname, homeworld and age, in order --}}
                    {{#each data}}
                    <tr>
                        <td>{{this.id}}</td>
                        <td>{{this.dropoff_id}}</td>
                        <td>{{this.weight}}</td>
                        <td>{{this.recyclable_weight}}</td>
                        <td><button onclick="deletePerson({{this.id}})">Delete</button></td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
        </div>

        {{!-- FIRST ASIDE FOR INSERTING DATA --}}
        <aside class="filter-container addingdata">
            <h2>Insert</h2>
            <form id="add-dropoff-orders-form" method="POST" action="/add-dropoff-orders-form">

              <div class="filter-input-container">
                  <label class="filter-input-label" for="input-id">Customer ID:</label>
                  <div class="filter-input-element">
                  <input type="number" name="input-id" id="input-id">
                  </div>
              </div>
            
              <div class="filter-input-container">
                  <label class="filter-input-label" for="input-weight">Weight</label>
                  <div class="filter-input-element">
                  <input type="number" name="input-weight" id="input-weight">
                  </div>
              </div>    

            <div class="filter-input-container">
                <label class="filter-input-label" for="input-r-weight">Recyclable Weight</label>
                <div class="filter-input-element">
                <input type="number" name="input-r-weight" id="input-r-weight">
                </div>
            </div>

                <input class="submit-button action-button" type="submit">
            </form>
        </aside>
    </main>
  </body>
</html>
